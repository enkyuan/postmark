// TODO: auth with magic link (passwordless)

import React from 'react'
import { AuthForm } from '../components/AuthForm' 

const Auth = () => {
  return (
    <AuthForm />
  )
}

export default Auth;
